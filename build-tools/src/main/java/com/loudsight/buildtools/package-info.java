/**
 * Shared static-analysis rulesets (PMD, SpotBugs) resolved from the plugin classpath by
 * {@code com.loudsight:parent}. This module carries only resource files under
 * {@code src/main/resources} - no compiled code - so this file exists solely to give
 * maven-javadoc-plugin a compilation unit to document, since Maven Central requires a
 * javadoc jar for every jar-packaging artifact.
 */
package com.loudsight.buildtools;
